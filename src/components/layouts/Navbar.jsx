import React from 'react'

const Navbar = ({activeMenu}) => {
  return (
    <div className="">
         <button
            className=""
            onClick={()=>{
                setOpenSideMenu(!setOpenSideMenu)
            }}
        >
            
        </button>
    </div>
  )
}

export default Navbar